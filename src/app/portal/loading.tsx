import { PageSkeleton } from "@/components/portal/Skeleton";

/** Shown the instant a portal link is tapped, inside the app frame, until the page arrives. */
export default function Loading() {
  return <PageSkeleton />;
}
